@inject IConfiguration Configuration

@{
    var gaMeasurementId = Configuration.GetValue<string>("GA_MEASUREMENT_ID") ?? string.Empty;
    var gaContainerId = Configuration.GetValue<string>("GA_CONTAINER_ID") ?? string.Empty;

    if (gaMeasurementId != "")
    {
        <script>
            window.GA_MEASUREMENT_ID = '@gaMeasurementId';
            window.GA_CONTAINER_ID = '@gaContainerId';
        </script>
    }
}